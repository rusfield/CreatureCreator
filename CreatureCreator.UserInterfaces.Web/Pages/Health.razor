@page "/health"
@using CreatureCreator.Infrastructure.Services

@inject CreatureCreatorService Service

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        Service.LaunchWorldServer();
        base.OnAfterRender(firstRender);
    }
}
